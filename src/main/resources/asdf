;; Keyboard Macro Editor.  Press C-c C-c to finish; press C-x k RET to cancel.
;; Original keys: C-s pref RET M-f 2*C-f 2*M-d C-x o (* DEL / SPC C-y SPC 1.17) M-x eval TAB pr TAB RET C-p C-k C-x o C-y

Command: layout-macro
Key: 

Macro:

C-s			;; isearch-forward
layout			;; self-insert-command * 4
RET			;; newline
M-f			;; forward-word
2*C-f			;; forward-char
2*M-d			;; kill-word
C-x o			;; other-window
(*			;; self-insert-command * 2
DEL			;; delete-backward-char
/			;; nxml-electric-slash
SPC			;; self-insert-command
C-y			;; yank
SPC			;; self-insert-command
1.17)			;; self-insert-command * 5
M-x			;; execute-extended-command
eval			;; self-insert-command * 4
TAB			;; indent-for-tab-command
pr			;; self-insert-command * 2
TAB			;; indent-for-tab-command
RET			;; newline
C-p			;; previous-line
C-k			;; kill-line
C-x o			;; other-window
C-y			;; yank
